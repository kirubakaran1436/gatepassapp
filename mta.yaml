_schema-version: "3.2"
ID: gatepass
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: gatepass-destination-content
  type: com.sap.application.content
  requires:
  - name: gatepass-destination-service
    parameters:
      content-target: true
  - name: gatepass-repo-host
    parameters:
      service-key:
        name: gatepass-repo-host-key
  - name: gatepass-uaa
    parameters:
      service-key:
        name: gatepass-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: gatepass_html_repo_host
          ServiceInstanceName: gatepass-html5-srv
          ServiceKeyName: gatepass-repo-host-key
          sap.cloud.service: gatepass
        - Authentication: OAuth2UserTokenExchange
          Name: gatepass_uaa
          ServiceInstanceName: gatepass-xsuaa-srv
          ServiceKeyName: gatepass-uaa-key
          sap.cloud.service: gatepass
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: gatepass-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: gatepass-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - gatepass.zip
      name: gatepass
      target-path: resources/
- name: gatepass
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: gatepass-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: gatepass-destination-service
    service-plan: lite
- name: gatepass-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: gatepass-xsuaa-srv
    service-plan: application
- name: gatepass-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: gatepass-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
